/**
 * Token giao diện — seller chrome (khớp sellerUiDemo).
 */
/** Brand vàng nhạt (amber) — thử theme; chữ trên brand dùng tối. */
export const BRAND_HEX = '#e6c75a';
export const BRAND_HEX_LIGHT = '#f5cc30';
export const BRAND_HEX_DARK = '#c9a83a';
export const ON_BRAND = '#2c2416';

/** Padding ngang chuẩn màn list / form / detail (demo: 20). */
export const CANVAS_SCREEN_PAD_H = 20;

export const RADIUS = {
  sheet: 22,
  lg: 20,
  md: 14,
  sm: 10,
  control: 10,
  pill: 24,
} as const;

/** Khoảng đệm / typography thẻ danh sách mobile — CompactListMobileCard. */
export const LIST_CARD = {
  padH: 12,
  padV: 12,
  lineGap: 4,
  leadingGap: 10,
  headerPadH: 12,
  headerPadV: 8,
  bodyPadH: 12,
  bodyPadV: 8,
  bodyGap: 4,
  headerGap: 8,
  listGap: 10,
  fontTitle: 14,
  fontBody: 13,
  fontMeta: 12,
  fontAccent: 14,
  thumbSize: 36,
  actionBtn: 28,
} as const;
