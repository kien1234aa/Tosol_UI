import React from 'react';

export type ListCardInlineActionsProps = {
  onView?: () => void;
  onDelete?: () => void;
};

/** Nút xem/xóa trên thẻ list — đã tắt; mở chi tiết bằng chạm cả thẻ. */
export function ListCardInlineActions(_props: ListCardInlineActionsProps) {
  return null;
}
